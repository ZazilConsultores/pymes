<?php 
	$producto = $this->producto;
	$multiplos = $this->multiplos;
	$formulario = $this->formulario;
	$formulario->setAction($this->url(array("module"=>"inventario", "controller"=>"multiplos", "action" => "alta"), null, true));
	
?>
<section class="row">
	<article>
		<header>
			<h3>Multiplos:  <?php echo $producto->getProducto() ?></h3>
		</header>
		<div>
			<a class="btn btn-success" href="<?php echo $this->url(array("module"=>"inventario","controller"=>"multiplos","action"=>"alta","idProducto"=>$producto->getIdProducto()),null,true); ?>">
				<span class="glyphicon glyphicon-plus"></span> Nuevo Multiplo
			</a>
			<table class="table table-condensed table-striped">
				<thead>
					<th>Cantidad</th>
					<th>Unidad</th>
					<th>Abreviatura</th>
					<th>Administrar</th>
					
				</thead>
				<tbody>
					<?php foreach ($multiplos as $multiplo) : ?>
					<tr>
						<td><?php echo $multiplo->getCantidad(); ?></td>
						<td><?php echo $multiplo->getUnidad(); ?></td>
						<td><?php echo $multiplo->getAbreviatura(); ?></td>
						<td>
							<a class="btn btn-info" href="<?php echo $this->url(array("module"=>"inventario","controller"=>"multiplos","action"=>"admin","idMultiplos"=>$multiplo->getIdMultiplos()),null,true); ?>">
								<span class="glyphicon glyphicon-cog"></span> Administrar
							</a>
						</td>
					</tr>
					<?php endforeach ?>
				</tbody>
			</table>
		</div>
	</article>
</section>
