<?php 	$producto = $this->producto;
		$multiplos = $this->multiplos;
		$formulario = $this->formulario;
		$formulario->setAction($this->url(array("module"=>"inventario", "controller"=>"multiplos", "action" => "alta"), null, true));
?>
<section class="row">
	<div class="panel panel-default">
		<div class="panel-heading">
			<h3 class="panel-title">
				Multiplos del producto: <?php echo $producto->getProducto() ?>.
			</h3>
		</div>
		<div class="panel-body">		
			<a class="btn btn-success" href="<?php echo $this->url(array("module"=>"inventario","controller"=>"Multiplos","action"=>"alta","idProducto"=>$producto->getIdProducto()),null, true) ?>">
				<span class="glyphicon glyphicon-plus"></span> Nuevo Multiplo
			</a>
			<table class="table table-striped table-condensed">
				<thead>
					<tr>
						<th>Producto</th>
						<th>Multiplo</th>
						<th>Administrar</th>
					</tr>
				</thead>
				<tbody>
					<?php foreach ($multiplos as $multiplo) : ?>
					<tr>
						<td><?php echo $producto->getProducto() ?></td>
						<td><?php echo $multiplo->getMultiplo() ?></td>
						<td>
							<a class="btn btn-info" href="<?php echo $this->url(array("module"=>"inventario", "controller"=>"multiplos", "action" => "admin", "idMultiplos" => $multiplo->getIMultiplos()), null, true); ?>">
								<span class="glyphicon glyphicon-cog"></span> Administrar
							</a>
						</td>
					</tr>
					<?php endforeach; ?>
				</tbody>
			</table>
		</div>
	</div>
</section>
