<?php 
	$encuesta = $this->encuesta; 
	$estatus = Zend_Registry::get("estatus");
?>
<section class="row">
	<header>
		<h3>Encuesta: <?php echo $encuesta->getNombre(); ?>  
			<a href="<?php echo $this->url(array("module"=>"encuesta","controller" => "generador", "action" => "index", "idEncuesta" => $encuesta->getIdEncuesta()), null, true) ?>" class="btn btn-success">
				Generar
				<span class="glyphicon glyphicon-cog"></span>
			</a>
		</h3>
	</header>
	<table class="table table-striped">
		<thead>
			<th>Nombre Clave</th>
			<th>Fecha</th>
			<th>Estatus</th>
			<th>Administrar</th>
			<th>Secciones</th>
			<th>Descripción</th>
		</thead>
		<tbody>
			<tr>
				<td><?php echo $encuesta->getNombreClave(); ?></td>
				<td><?php echo $encuesta->getFecha(); ?></td>
				<td><?php echo $estatus[$encuesta->getEstatus()]; ?></td>
				<td>
					<a href="<?php echo $this->url(array("module"=>"encuesta","controller" => "index", "action" => "admin", "idEncuesta" => $encuesta->getIdEncuesta()), null, true) ?>" class="btn btn-warning">
						<span class="glyphicon glyphicon-cog"></span>  Administrar
					</a>
				</td>
				<td>
					<a href="<?php echo $this->url(array("module"=>"encuesta","controller" => "index", "action" => "secciones", "idEncuesta" => $encuesta->getIdEncuesta()), null, true) ?>" class="btn btn-warning">
						<span class="glyphicon glyphicon-cog"></span>  Secciones
					</a>
				</td>
				<td><?php echo $encuesta->getDescripcion(); ?></td>
			</tr>
		</tbody>
	</table>
</section>